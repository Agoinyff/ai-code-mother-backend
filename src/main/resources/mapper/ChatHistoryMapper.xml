<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yff.aicodemother.mapper.ChatHistoryMapper">

    <resultMap id="BaseResultMap" type="com.yff.aicodemother.model.entity.ChatHistory">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="message" column="message" jdbcType="VARCHAR"/>
            <result property="messageType" column="messageType" jdbcType="VARCHAR"/>
            <result property="appId" column="appId" jdbcType="BIGINT"/>
            <result property="userId" column="userId" jdbcType="BIGINT"/>
            <result property="createTime" column="createTime" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="updateTime" jdbcType="TIMESTAMP"/>
            <result property="isDelete" column="isDelete" jdbcType="TINYINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,message,messageType,
        appId,userId,createTime,
        updateTime,isDelete
    </sql>

    <!-- 分页查询指定应用的对话历史（用户查询） -->
    <select id="selectChatHistoryVoPage" resultType="com.yff.aicodemother.model.vo.ChatHistoryVo">
        SELECT
            id, message, messageType, appId, userId, createTime, updateTime
        FROM chat_history
        <where>
            isDelete = 0
            <if test="query.appId != null">
                AND appId = #{query.appId}
            </if>
            <!-- 权限校验：用户只能查询自己创建的应用的对话历史 -->
            <!-- 这里只返回指定应用的对话历史，权限在 Service 层校验 -->
        </where>
        ORDER BY createTime DESC
    </select>

    <!-- 管理员分页查询所有对话历史 -->
    <select id="selectChatHistoryVoPageForAdmin" resultType="com.yff.aicodemother.model.vo.ChatHistoryVo">
        SELECT
            id, message, messageType, appId, userId, createTime, updateTime
        FROM chat_history
        <where>
            isDelete = 0
            <if test="query.id != null">
                AND id = #{query.id}
            </if>
            <if test="query.appId != null">
                AND appId = #{query.appId}
            </if>
            <if test="query.userId != null">
                AND userId = #{query.userId}
            </if>
            <if test="query.messageType != null and query.messageType != ''">
                AND messageType = #{query.messageType}
            </if>
            <if test="query.message != null and query.message != ''">
                AND message LIKE CONCAT('%', #{query.message}, '%')
            </if>
        </where>
        ORDER BY createTime DESC
    </select>

</mapper>
